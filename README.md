# Import Old Users
A tool to import old cuhHub players from `server_config.xml` auth lists.

Add a `server_config.xml` file to root directory and run `main.py` to use.

Use `import.py` after to push players to API.

Rename `example.env` to `.env` and modify the variables before use.